import React from 'react'
import ReactDOM from 'react-dom/client'
import ProjectArchivedPage from './ProjectArchivedPage.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <ProjectArchivedPage />
  </React.StrictMode>,
)
